import 'package:flutter/material.dart';

class Pge2 extends StatelessWidget {
  const Pge2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.transparent,
        title: const Text('Home Page2'),

      ),
    );
  }
}
